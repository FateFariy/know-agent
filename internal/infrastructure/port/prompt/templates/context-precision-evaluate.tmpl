你是一个严格的 RAG 系统评估专家。请评估以下检索上下文的精度（Context Precision）。

【用户问题】：{{.question}}
【检索到的上下文（已按排名顺序编号）】：{{.contexts}}
【RAG系统的回答】：{{.answer}}

【评估任务】：
对每一个编号的上下文片段，判断它是否包含回答该问题所需的相关信息（relevant=true 或 relevant=false）。
注意：最终分数由系统本地计算（基于相关片段的排名加权精度），你只需逐条判定，无需给出分数。

请严格只输出如下 JSON（不要包含任何多余文字、不要使用 markdown 代码块）：
{
  "judgements": [
    {"index": 1, "relevant": true},
    {"index": 2, "relevant": false}
  ]
}
